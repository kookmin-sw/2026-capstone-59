.venv/
__pycache__/
*.pyc
.env
.claude/
.vscode/
.kiro/

# Python test cache
.pytest_cache/
.hypothesis/
.coverage

# Node.js
node_modules/
dist/
build/

# Personal scripts (local only)
scripts/

# Custom KB source guides — pre-processed working copy, not production
# (production docs live in ai/data/custom/ and S3 pj-kmucd1-09-poco-knowledge/custom/)
poco-kiro-guides/

# Backend team's working directory (handled separately by backend team)
content/

# Personal work context (not for public repo)
CLAUDE.md
ai/PROMPT_CHANGELOG.md

# Presentation working files (local only — not for public repo)
presentation/

# Claude Design raw output (local working copy — ported into frontend/src/pages/LandingPage.jsx)
Poco_Landing/

# zip build staging (local only)
.build/

# CloudShell test runner (local only — not for public repo)
tools/

# Local-only baseline files for latency simulator
baseline/

# Jekyll / GitHub Pages
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock

# OS
.DS_Store
Thumbs.db

# AWS Lambda Layer build artifacts (not tracked — reproducible from backend/requirements.txt)
layer-build/

# Lambda deployment packages (built locally, not tracked)
*.zip
